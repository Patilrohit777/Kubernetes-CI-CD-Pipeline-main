apiVersion: apps/v1
kind: Deployment
metadata:
  name: kubernetes-ci-cd-pipeline-main
spec:
  replicas: 2
  selector:
    matchLabels:
      app: kubernetes-ci-cd-pipeline-main
  template:
    metadata:
      labels:
        app: kubernetes-ci-cd-pipeline-main
    spec:
      containers:
        - name: kubernetes-ci-cd-pipeline-main
          image: patilr566/kubernetes-ci-cd-pipeline-main
          ports:
            - containerPort: 80